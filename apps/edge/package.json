{
  "name": "@jcb/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --legal-comments=none --platform=browser --format=esm --target=es2024 --outfile=dist/index.js",
    "deploy": "pnpm build && wrangler deploy"
  },
  "dependencies": {
    "@jcb/contracts": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@jcb/application": "workspace:*",
    "@cloudflare/workers-types": "5.20260801.1",
    "esbuild": "0.28.1",
    "wrangler": "4.118.0"
  }
}
